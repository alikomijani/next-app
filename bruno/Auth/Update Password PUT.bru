meta {
  name: Update Password PUT
  type: http
  seq: 6
}

put {
  url: {{baseUrl}}/api/auth/password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  content-type: application/json
}

body:json {
  {
    "currentPassword": "{{password}}",
    "newPassword": "new-password123"
  }
}

docs {
  Updates the current user's password with PUT.
}
