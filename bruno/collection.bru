meta {
  name: next-app
  type: collection
  seq: 1
}

docs {
  # next-app API

  Bruno collection for the Next.js route handlers in `src/app/api`.

  Use the `Local` environment and set `baseUrl` to your running app.
}
