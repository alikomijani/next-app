# Bruno API Collection

Open this `bruno` folder in Bruno and select the `Local` environment.

Set `accessToken` after login if you want to call protected auth routes with a bearer token. The app also sets auth cookies on login and refresh.

Catalog IDs can be copied from create responses into `categoryId`, `brandId`, `productId`, and `variantId`. Product list filters accept category/brand ObjectIds or slugs.
